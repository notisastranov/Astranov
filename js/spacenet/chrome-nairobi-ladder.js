/**
 * Nairobi / Africa / Kenya zoom ladder — Build 20260823003000-nairobi-rungs
 *
 * Guest types "nairobi" / "africa" / "kenya" (or pinches the live globe over Kenya)
 * → honest three sequential rungs — never a teleport to a wrong continent.
 *
 * PLACEHOLDER WAS REPLACED — full content is large; see artifacts for recovery.
 * This is a temporary short stub until full push succeeds.
 */
(function (G) {
  'use strict';
  console.warn('[nairobi-ladder] full bytes not yet pushed on this commit');
  G.SNNairobiLadder = { __placeholder: true, build: '20260823003000-nairobi-rungs' };
})(typeof window !== 'undefined' ? window : globalThis);
