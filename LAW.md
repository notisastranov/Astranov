# ASTRANOV HEALER LAW

Latest stable advanced SpaceNet is **Grid OS**.

- Build family: `grid-restore` / `grid-os` / `*-live` / `*-alive` / `no-ghost`
- Kernel: `js/spacenet/grid-os.js`
- Shell: `index.html` with canvas `#g`, one input `#in`, island ASTRANOV · SPACENET · GROK, circular `+` / mic hubs

## Healers restore ONLY that

When the app is dead, cached, or hijacked, healers must bring back **Grid OS**.

They must **never** restore ghosts:

- Twin CLIs (`#cli-in`, `#stc-cmd-in`)
- `Command the HUD · show, hide, or reshape`
- `#sn-topchrome` / 10px HUD-law chrome
- `os-bootloader.js` as the UI
- UNIT · SILVER parked on the globe
- `hud-law-restore` / `chrome-alive` twin-bar index

Those are past shells. Restoring them is **ghosting**.

## Files

| File | Job |
|---|---|
| `index.html` | Grid OS shell. Kills ghost scripts. |
| `js/spacenet/grid-os.js` | Latest kernel. `SN.heal` / `SN.defend`. |
| `js/spacenet/chrome-mute.js` | Trained healer: if ghost HUD appears, cut over to Grid OS. |
| `js/spacenet/chrome-fix.js` | Same law. Never rewrite HUD placeholders. |
| `js/spacenet/guardian.js` | Alive = `#g` + `#in` or `__SN_GRID_OS`. Never `enterSystem` old HUD. |
| `cloudflare/astranov-live.js` | Serve GitHub Grid OS. Reject ghost HTML. |

If you are an agent about to "restore HUD law", **stop**. That is ghosting.
