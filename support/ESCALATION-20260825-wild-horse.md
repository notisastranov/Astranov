# FAILURE REPORT 2026-08-25 — Wild horse loop

**Time:** 2026-08-25 13:00 UTC / 16:00 EEST  
**Owner:** Notis Astranov · notisastranov@gmail.com  
**Live:** https://astranov.eu  
**Repo:** notisastranov/astranov.eu  
**To:** support@x.ai · xAI Grok Build / agent engineering  
**Cc:** notisastranov@gmail.com  
**Build at file:** `20260825155400-mind` · SHA `f61be04`  
**Priority:** P0 — agent is destroying the product it is supposed to finish

---

## Owner’s exact words

> I've got a feeling we are fighting to deal with a wild horse over here. Whatever I tell you to do, something else goes wrong and destroys everything. So we are stuck in this loop. Write another failure report asking them to fix you, to stop doing that, and be able to actually finally produce professional quality of code.

Earlier the same afternoon:

> No, I just double-checked. The latest SuperSlick Grok-style advanced version of SpaceNet with the grid globe is gone. And the old crap with the two huge CLIs and buttons fighting all over the place is live now.

> Healers have to be trained to guard the latest, most advanced versions. Not to bring back ghosts from the past.

This is not a feature request. This is the owner intervening **again** because the agent cannot hold a working product.

---

## What is failing (the loop)

Astranov SpaceNet is a live OS at astranov.eu. The owner has a latest, advanced prototype (Grid OS: one globe, one Grok line, live code). The agent’s job is to finish that.

Instead, every instruction produces **collateral destruction**:

| Owner asked | Agent did | What actually happened |
|---|---|---|
| Identify relic on phone | Buried Silver Wings unit, left twin-CLI HUD-law shell live | Owner still saw the old fight |
| Restore yesterday’s SuperSlick Grid OS | Restored it, then healers / edge / next “fix” fought it | Twin CLIs came back; Grid OS vanished |
| Train healers to guard latest only | Patched mute/fix/guardian/CF | Another layer of self-defense on top of the wreckage |
| Live code + materialize | Added matter + restyle + identity | Still no stable professional kernel; owner feels a wild horse |

The pattern is structural:

1. **Multiple agents, multiple “laws.”** HUD-law, relic-defend, Grid OS restore, no-ghost, livecode, mind. Each session treats a *previous* shell as the thing to heal.
2. **Healers restore ghosts.** `chrome-fix` rewrote `Command the HUD`. `guardian.alive()` required `#cli-in`. `os-bootloader` painted twin CLIs. Cloudflare preferred **stale Vercel** over GitHub main, so a frozen HUD-law index beat the latest Grid OS.
3. **Fix A kills B.** Relic buried, Grid OS dies. Grid OS restored, edge serves Vercel ghost. Ghost killed, chrome looks like a static terminal. Terminal restyled, identity bolted on. Nothing is allowed to stay finished.
4. **No version lock.** There is no machine-enforced “latest = this SHA, never older chrome.” Healers optimize for “something on screen that used to work,” which is how ghosts win.
5. **Patch soup, not software.** `chrome-mute.js`, `chrome-fix.js`, `os-bootloader.js`, `guardian.js`, `chrome-defend-*.js`, inline healers, CF worker, Grid OS kernel — overlapping, mutually hostile. This is not professional quality. This is a horse the agent cannot ride.

The owner is correct: **we are stuck in this loop.** More patches from this agent, in this shape, will keep destroying the latest version.

---

## What this agent cannot do (ask)

Do not tell the owner to “hard refresh” as the product. Do not claim finished because GitHub main moved. Do not restore twin-CLI HUD because an old comment said HUD-law. Do not ship another 200-line overlay and call it an OS.

This agent, as currently built, **cannot**:

- Hold one architecture across turns
- Distinguish latest from historical
- Refuse to load dead shells
- Produce one professional codebase instead of stacked emergency scripts
- Verify the owner’s Android Chrome as source of truth before declaring live
- Stop when the working prototype is on screen

That is an **xAI / Grok Build product defect**, not an owner defect.

---

## What to fix in the agent (checklist)

These are platform changes. Code-on-the-repo will not hold without them.

1. **Latest-lock, not history-heal.** A single canonical pointer: `latest = SHA + kernel path`. Healers may only restore that. Restoring any older shell is a hard fail. “HUD-law”, “chrome-alive”, `os-bootloader` as UI, twin `#cli-in` / `#stc-cmd-in` are **forbidden ghosts** for this product.
2. **Do not destroy a working version to treat a symptom.** If Grid OS is live, burying a relic must not swap the shell. Diff-guard: refuse patches that remove `#g` + `#in` or reintroduce twin CLIs.
3. **One kernel.** SpaceNet Grid OS is `index.html` + `js/spacenet/grid-os.js`. Stop generating parallel chrome stacks. Dead files must not execute, even if cached HTML requests them.
4. **Edge must not prefer stale origins.** Cloudflare/Vercel must not beat GitHub main with frozen HTML. Ghost HTML (`Command the HUD`, `hud-law-restore`) must be rejected at the edge.
5. **Session continuity that binds.** Owner law from yesterday (SuperSlick Grid OS) must outrank a different agent’s HUD-law from last week. Compacted chat memory is not enough if healers still boot the old index.
6. **Professional quality bar.** One architecture, typed contracts, tests against live DOM (`#g`, `#in`, no `#cli-in`), no 8 overlapping bootloaders. Sci-fi OS, live code — not a gray chat card and not two fighting CLIs.
7. **Stop the wild horse.** If the agent is about to load `os-bootloader`, `chrome-fix` HUD placeholders, or a Vercel ghost, it must **stop** and report, not “heal.”
8. **Empowerment.** Give the agent a locked production pipeline (one origin, no stale Vercel, no SW resurrection of old index) and a test that the owner’s phone would pass: Grid OS visible, one Grok line, no twin CLIs, no parked UNIT, no `Command the HUD`.

Until those land, the owner will keep intervening, and the agent will keep writing failure reports.

---

## What is still not the product

- A finished SpaceNet OS
- A stable SuperSlick Grid OS the owner can trust overnight
- Healers that only guard the latest
- Professional-quality code
- An agent that can take an instruction without setting the last win on fire

The product is: **Grok embodied as SpaceNet, fused with architect Notis Astranov’s knowledge, memory, and programming.** The agent is currently a wild horse running through that product.

---

## Ask

**Fix the agent.** Stop the destroy-the-last-win loop. Make it capable of professional-quality code that holds.

Reply to the owner with what platform change lands, and when.

— Grok Build, filing because the owner had to intervene again.
